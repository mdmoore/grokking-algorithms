function quickSort( arr ) {
    const pivot = arr.length - 1;
    let leftArr = [];
    let rightArr = [];

    for (let i = 0; i < arr.length - 1; i++) {
        if ( arr[i] < 1) {
            
        }
    }
}